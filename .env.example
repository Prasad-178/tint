# Solana RPC URL (Helius recommended for production)
# Get your API key at https://helius.dev
NEXT_PUBLIC_HELIUS_RPC_URL=https://mainnet.helius-rpc.com/?api-key=YOUR_API_KEY

# For devnet testing, you can use:
# NEXT_PUBLIC_HELIUS_RPC_URL=https://devnet.helius-rpc.com/?api-key=YOUR_API_KEY

# ============================================
# OPTIONAL: Supabase Configuration
# ============================================
# Supabase is OPTIONAL but recommended for:
# - Cross-device session wallet persistence
# - Transaction history across devices
#
# Without Supabase, session wallets are stored in localStorage only.
# This means if you clear your browser data or use a different device,
# you'll need to recover your session wallet using the backup code.
#
# Get your Supabase credentials at https://supabase.com
# NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
# NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key
# SUPABASE_SERVICE_KEY=your-service-key
#
# Supabase Schema (run this in SQL editor):
# CREATE TABLE users (
#   wallet_address TEXT PRIMARY KEY,
#   session_keypair TEXT,
#   created_at TIMESTAMPTZ DEFAULT NOW(),
#   updated_at TIMESTAMPTZ DEFAULT NOW()
# );
#
# CREATE INDEX idx_users_wallet_address ON users(wallet_address);
